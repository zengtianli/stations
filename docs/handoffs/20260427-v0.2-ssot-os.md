# Handoff · ssot-os · v0.2

> 这是 **ssot-os**（~/Dev 模块化 + 可复用 + 可观察）项目的 **v0.2**（共 6 轮 → v1.0）
> 终点：[ROADMAP § v1.0 验收](./ROADMAP.md#v10-验收终点画像) 6 项画像
> 2026-04-27 · v0.2 ✅ ship — A1 收尾 + C1 ops-console + 用户新诉求 "augment 瘦身" 3 个轻量收益
> 上轮归档：[20260427-v0.1-ssot-os.md](./stations/docs/handoffs/20260427-v0.1-ssot-os.md)
> 全方案 plan：[~/.claude/plans/1-augment-sequential-spring.md](file:///Users/tianli/.claude/plans/1-augment-sequential-spring.md)

## 本轮闭环 ✅

### v0.2-1.1 撤 indexable + zdwp retype  (commit `e3e915c` @ tools/configs)
- `dev-scripts` (3 .py / 208K) → `indexable=false`（噪音 > 信号）
- `ticket-reminder` (1 file / 168K) → `indexable=false`
- `zdwp` (149 code / 2.6K files = 6%) → `type=docs`（仍 `indexable=true`，本质是 docs/data 仓）
- **24 → 22 indexable**（41 - 19 false）

### v0.2-1.2 5 个 `.augmentignore`（gitignore 语法的二级过滤）

augment 默认 honor `.gitignore`，但部分大仓真代码 5-12G。`.augmentignore` 是显式排除超越 gitignore 的工具：

| repo | commit | 排除核心 |
|---|---|---|
| `stations` | `9e02879` | `_archive/` + `*/data/` + `*/logs/` + `audiobook/data/audio` + 显式 `**/.next/.venv/__pycache__/dist/build/node_modules/` |
| `labs/hydro-apps` | `e8b9d6f` | `**/target/` + `**/dist/` + `**/node_modules/` + `**/data/` + `release/` |
| `labs/cockpit` | `414b806` | `src-tauri/target/` + `node_modules` + `dist/data/__pycache__/` |
| `tools/configs` | `e3e915c` | `nginx/out/` + `_archive/` + `_dotfiles/nvim/.git/` + 草稿/缓存 |
| `tools/doctools` | (已 tracked) | `.venv/` + `__pycache__/` + `templates/_samples/` |

### v0.2-1.3 auggie_warmup tiered timeout + retry-failed  (commit `c2a3c7d` @ devtools)
- `TIMEOUT_S=90` → 分级 `_tier_timeout(abs_path)`：
  - `< 500M → 90s`
  - `< 5G → 180s`
  - `>= 5G → 300s`
- 新增 `--retry-failed` flag：读 health.json 只重跑 `status != 'ok'`，保留 ok 记录
- `by_id_seed` 自动剔除已撤 indexable 的 stale 记录
- ROADMAP A1 acceptance 改分级（commit `1037286` @ ~/Dev）：原"全部 ≥ 0.7"改"小中仓 ≥ 0.7；大仓允许 cold-start 失败"

### v0.2-1.4 ops-console `/auggie/workspaces` 表  (commit `9e02879` @ stations)
- 新建 `stations/ops-console/app/auggie/workspaces/{page,table}.tsx`（41 ws + health 联合渲染）
- 列：id · type · indexable · github · health · duration · relevance · last_warmup · notes
- filter：query / type chips / indexable on/off
- `deploy.sh`: scp 加 `auggie-workspaces.json` + `auggie-health.json`
- `ops-console.yaml`: 加 `/auggie/workspaces` nav 入口；旧 `/auggie` match `prefix → exact` 防 sub-page 高亮错位
- **Live**: `dashboard.tianlizeng.cloud/auggie/workspaces`（部署后启用，预计 v0.2 收尾时）

## 关键 v0.1 → v0.2 改善

**v0.2 22 indexable 全跑完结果（21.7 min total）**：

| 状态 | 数 | workspaces |
|---|---|---|
| ✅ ok 0.85 | **19** | stations 5G(114s) + hydro-apps 12G(77s) + cockpit 4G(63s) + doctools 474M(82s) + hydro-qgis 211M(77s) + llm-finetune 918M(58s) + mindmap 939M(64s) + analysis-hermes(80s) + resume(54s) + bids 1.7G(85s) + eco-flow 2.8G(102s) + reports(62s) + zdwp 595M(91s) + (前 6 ok 保留：cc-configs / devtools / hammerspoon / mactools / raycast / auggie-dashboard) |
| 🟡 timeout | **3** | configs 11M / vps 1.1M / international-assets 74M（**全是小仓，后端节流非 size 问题**） |

**v0.1 → v0.2 救回**：stations / hydro-apps / cockpit / doctools 这 4 个之前 90s timeout 现全 ok（tiered timeout + `.augmentignore` 双效验证）。

**关键诊断**：configs 11M / vps 1.1M / international-assets 74M 极小仓 timeout —— 真因不是 size 而是 augment 后端 cold-start 排队/节流。`.augmentignore` 救得了大仓，救不了后端波动。这是 augment 服务质量问题。按 v0.2 调整后的 acceptance："小中仓 ≥ 0.7" — 这 3 个不达标的可在后续会话用 `--retry-failed` 再跑（成本 < 5 min），不阻塞 v0.2 ship。

## v0.3 评估 — 本会话已 evidence-based 调研，未实施

按 [plan §2](file:///Users/tianli/.claude/plans/1-augment-sequential-spring.md#2--phase-v03--重复合并互独立--任意顺序-ship)：

| 子项 | 风险 | 本轮 evidence | 建议 |
|---|---|---|---|
| **2.1 hammerspoon 双份** | 低 | 0 外部引用 + `~/.hammerspoon` symlink 指权威 (`tools/hammerspoon`) + 非 submodule + configs CLAUDE.md 自标 TODO | ✅ 可立即做（4.2M 释放） |
| **2.2 cc-options 外层 .venv** | 中 | `daily_update.sh` 用 `/opt/homebrew/bin/python3`；系统 python `pip list` 显示 `snaptrade-python-sdk` 已装；但 `import snaptrade_python_sdk` 失败（**module name 待 verify**，可能是 `snaptrade_client`） | ⚠️ 先 grep cc-options 实际 import 名 +verify 系统 python 真有，再决定 |
| **2.3 devtools + doctools .venv 合并** | 中-高 | 两 venv 共 943M；deps 19 个完全重合；**0 外部引用** verified | ✅ 可做（按 plan §2.3 四子步） |

## 关键文件

| 文件 | commit / 状态 | 说明 |
|---|---|---|
| `/Users/tianli/Dev/ROADMAP.md` | `1037286` | A1 acceptance 分级 |
| `/Users/tianli/Dev/tools/configs/auggie-workspaces.yaml` | `e3e915c` | 41 ws · 22 indexable |
| `/Users/tianli/Dev/tools/configs/auggie-health.json` | (warmup 写入中) | 22 records，部署 ops-console 时 scp |
| `/Users/tianli/Dev/devtools/lib/tools/auggie_warmup.py` | `c2a3c7d` | tiered + retry-failed |
| `/Users/tianli/Dev/stations/ops-console/app/auggie/workspaces/` | `9e02879` | C1 表（page + table） |
| `/Users/tianli/Dev/stations/{,/labs/hydro-apps,/labs/cockpit}/.augmentignore` | 各 repo | 3 ws（doctools 已 tracked，configs 内含） |
| `/Users/tianli/.claude/plans/1-augment-sequential-spring.md` | (plan file) | v0.2 + v0.3 + v0.4 全方案 |
| `/Users/tianli/Dev/stations/docs/handoffs/20260427-v0.1-ssot-os.md` | (归档) | 上轮 v0.1 完整 |

## 下个会话启动

```
/warmup
```

读完看到 v0.2 ✅ + v0.3 评估完成。然后选：

**A. 推 v0.3（重复合并）**
1. **2.1 hammerspoon 合并**（最低风险，~5 min）：grep 0 引用 verified → `rm -rf tools/configs/hammerspoon` → 改 configs CLAUDE.md → commit
2. **2.2 cc-options venv** 决策：先 `grep -rn "import\|from" ~/Dev/stations/cc-options/*.py` 看真 import → verify 系统 python 真有该 module → 删/不删
3. **2.3 tools 共享 venv**：按 plan §2.3 4 子步（盘点 → workspace skeleton → 切换 → 烟测）

**B. 直接跳 v0.4 ops-console → web-stack monorepo**
- 先 pre-flight：ops-console 是否引 `@tlz/ui`？deploy convention？
- 风险中-高，feature toggle + canary

**C. 把 v0.3 也写细分 ROADMAP 阶段表，再启动**
- v0.3 现是 plan §2 概要，可先把它升格为 ROADMAP 正式阶段（标准化 v0.x 推进流程）

## 踩过的坑

### 1. configs 11M / vps 1.1M 仍 timeout
- 极小仓也 timeout —— 不是 size 问题，是 augment 后端 cold-start 节流/排队
- `.augmentignore` 救得了大仓（stations/hydro-apps/cockpit/doctools），救不了后端波动
- 教训：v0.4 / v0.5 可能需 retry-with-backoff 或 ad-hoc retry skill

### 2. ops-console.yaml `/auggie` match `prefix → exact`
- 加 sub-route `/auggie/workspaces` 后，旧 `/auggie` match=prefix 会让 nav 同时高亮 GitHub Repos + Workspaces
- 改 exact + 子路由独立条目，让 nav 状态正确

### 3. doctools `.augmentignore` 早就 tracked
- Write 工具写完 git status 显示 clean — 原以为 bug
- 真因：doctools repo 已 tracked 同名同内容文件（v0.0.x 时期遗留？）。我重写没改变内容
- 节省一次 commit。但 explore agent 之前 `find` 没扫到（**find 命令 bug 待查**）

### 4. devtools commit 多文件
- `git add lib/tools/auggie_warmup.py` 单文件，但 commit 显示 4 files changed
- 真因：devtools 装了 `git_auto_stage.sh` PostToolUse hook，把 site-navbar.html / menus.py / reflect-flags.log 等也 staged 了
- 不影响（都是合理改动），但 commit message 范围溢出。教训：检查 hook 副作用

### 5. cc-options venv 评估时假证据
- 先以为 `daily_update.sh PYTHON=/opt/homebrew/bin/python3` 等于"venv 不用"
- 但 `import snaptrade_python_sdk` 失败 + `pip list` 显示 PyPI 包 `snaptrade-python-sdk` 已装
- 真因：PyPI package name `snaptrade-python-sdk` 但 Python module name 可能是 `snaptrade_client`（待 verify）
- v0.3 删 cc-options venv 前必须先 grep 真 import name
