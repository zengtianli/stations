# logs

**站群时间线** — [logs.tianlizeng.cloud](https://logs.tianlizeng.cloud) (CF Access)

把 `~/Dev/*/HANDOFF.md` + `~/.claude/plans/*.md` + cc-evolution/changes.yaml 合并渲染成时间降序页面，替代旧的 journal / changelog 站。

```bash
bash deploy.sh   # 生成 + 部署
```
