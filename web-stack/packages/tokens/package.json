{
  "name": "@tlz/tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens SSOT — HSL CSS variables + 4 方向 accent + Inter + radius 1rem + keyframes",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind-preset": "./tailwind-preset.ts",
    "./globals.css": "./globals.css"
  },
  "scripts": {
    "build": "echo 'tokens: nothing to build (TS + CSS consumed directly)'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "@tlz/config": "workspace:*",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
